# Este workflow roda quando alguem ABRE um Pull Request.
# Ele NAO publica nada. Ele so confere.
# Por isso ele e o porteiro: nada entra na main sem passar por aqui.

name: Conferir Pull Request

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verificar:
    # Conferimos as DUAS licoes. Cada licao vive na sua pasta,
    # com as suas proprias regras (scripts/) e o seu proprio conteudo.
    name: Verificar ${{ matrix.licao }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        licao: [cicd, cicd-aula-02]
    steps:
      - name: Pegar o codigo do Pull Request
        uses: actions/checkout@v4

      - name: Rodar as regras do projeto
        working-directory: ${{ matrix.licao }}
        run: python3 scripts/check.py

      - name: Construir o site (so pra ver se constroi)
        working-directory: ${{ matrix.licao }}
        run: python3 scripts/build.py
